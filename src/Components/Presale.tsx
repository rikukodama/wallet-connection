import React from "react";
import MainVisual from "./MainVisual";

const Main: React.FC = () => {
    return (
        <div className="px-28 flex justify-between">
            <MainVisual />
        </div>
    );
};

export default Main;