import React from "react";
import MainVisual from "./MainVisual";

const Main: React.FC = () => {
    return (
        <div className="px-20 flex justify-between">
            <MainVisual />
        </div>
    );
};

export default Main;